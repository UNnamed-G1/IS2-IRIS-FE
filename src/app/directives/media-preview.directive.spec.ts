import { MediaPreviewDirective } from './media-preview.directive';

describe('MediaPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
